{
  "name": "readwalk",
  "version": "1.0.0",
  "description": "Navigate static web content using word and paragraph motions — a visual pacer for reading.",
  "main": "index.js",
  "scripts": {
    "start": "web-ext run --source-dir ./src --firefox-profile ./firefox-profile --keep-profile-changes --start-url about:debugging",
    "lint": "web-ext lint --source-dir ./src",
    "build": "web-ext build --source-dir ./src"
  },
  "keywords": [
    "reading",
    "navigation",
    "pacer",
    "firefox",
    "webextension"
  ],
  "author": "castaway",
  "license": "ISC",
  "devDependencies": {
    "web-ext": "^9.2.0"
  }
}